server:
  port: 8002
  
spring:
  application:
    name: ManagerMuse
    
  datasource:
    url: jdbc:mysql://localhost:3306/Jobs1?createDatabaseIfNotExist=true
    username: root
    password: India@123
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
